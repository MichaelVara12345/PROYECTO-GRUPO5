name: Grupo 5

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    env:
      plataforma: reniec
      region: piura
      clase: HDD
    
    runs-on: ubuntu-latest
    
    steps:
      - name: "Prueba de mensajes"
        run: echo "Hola Grupo 5, bienvenidos"
